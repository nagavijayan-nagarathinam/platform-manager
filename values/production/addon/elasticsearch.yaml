fullnameOverride: platform-elasticsearch

security:
  enabled: false

master:
  replicaCount: 1

data:
  replicaCount: 1
  persistence:
    enabled: true
    size: 20Gi

coordinating:
  replicaCount: 1

ingest:
  replicaCount: 1

metrics:
  enabled: true
