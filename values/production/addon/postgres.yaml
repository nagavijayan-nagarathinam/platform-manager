fullnameOverride: platform-postgres

auth:
  username: vote
  password: pgvote123
  database: voteapp

primary:
  persistence:
    enabled: true
    size: 2Gi
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 300m
      memory: 512Mi
